apiVersion: v2
name: secrets-demo
description: Demonstration workload that mounts an AWS Secrets Manager secret via IRSA + the Secrets Store CSI driver (ASCP), running under the cluster's default-deny network policy.
type: application
version: 0.1.0
appVersion: "1.0"
